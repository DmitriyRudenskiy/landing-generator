<style>
    .header_bg {
        width: 100%;
        min-height: 608px;
    }

    .headline {
        width: 100%;
        height: 100%;
        padding-top: 96px;
        color: #fff;
        text-align: center;
    }
</style>

<div class="header_bg" style="background: url('{{ bg }}') center no-repeat #ccc">
    <div class="headline">
        <div class="container">
            <h1>{{ title | nl2br }}</h1>
            <h2>{{ sub_title | nl2br}}</h2>
            <hr>

            <p style="font-size: 150%;">{{ description | nl2br}}</p></div>
        <p style="margin-top: 24px;">
            {% if button_url is not empty %}
                <a href="{{ button_url }}" target="_blank" class="btn btn-primary btn-lg">{{ button }}</a>
            {% else %}
                <button type="button" class="btn btn-primary btn-lg" data-toggle="modal" data-target="#get_phone">{{ button }}</button>
            {% endif %}
        </p>


        <div class="row" style="padding-top: 48px; padding-bottom: 24px;">
            <div class="col-md-offset-3 col-md-2 col-xs-12">
                <p class="text-center">
                    <img src="{{ pic_1 }}" alt="" style="max-width: 100px;"> {{ add_1 | nl2br }}
                </p>
            </div>

            <div class="col-md-2 col-xs-12">
                <p class="text-center">
                    <img src="{{ pic_2 }}" alt="" style="max-width: 100px;"> {{ add_2 | nl2br }}
                </p>
            </div>

            <div class="col-md-2 col-xs-12">
                <p class="text-center">
                    <img src="{{ pic_3 }}" alt="" style="max-width: 100px;"> {{ add_3 | nl2br }}
                </p>
            </div>
        </div>
    </div>
</div>